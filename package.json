{
  "name": "la-cupula",
  "version": "1.0.0",
  "description": "La Cúpula app",
  "main": "src/app.ts",
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  },
  "scripts": {
    "build": "cd server && npm run build && cd ../web-app && npm run build",
    "start:server": "cd server && npm run dev",
    "install": "cd server && npm install && cd ../web-app && npm install",
    "ci": "cd server && npm ci && cd ../web-app && npm ci",
    "test": "cd server && npm test"
  },
  "author": "Javier Sintes",
  "license": "ISC"
}
